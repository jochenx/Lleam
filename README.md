# veriground
A verification tool that checks LLM statements for truth. Grounding is assured by formals proofs, where the a theorem prover complements the LLM in mutually arriving at a proof of statement.
